{
  "name": "socket-server",
  "version": "1.0.0",
  "description": "this is socket server which transfer input data from web client to unity client",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "iconv": "^3.0.0",
    "nodemon": "^2.0.4",
    "socket.io": "^1.4.5"
  }
}
